# JLPT N1 読解トレーナー — 設定說明

小孩的英文 `reading.html` 完全不受影響。這支是**獨立的單人 N1 日文閱讀 app**:
自己的前端 `n1-reading.html`、自己的 Apps Script 後端、自己的 Google Sheet。

## 一、建立後端（Apps Script）

1. 開一份**新的** Google Sheet（給 N1 專用，別跟小孩英文那份混用）。
2. 在該 Sheet 上方選單：**擴充功能 → Apps Script**。
3. 把 `n1-apps-script/Code.gs` 的全部內容貼進去，取代預設的 `myFunction`。
4. 左邊 **專案設定（齒輪）→ 指令碼屬性 → 新增屬性**：
   - 名稱：`GEMINI_API_KEY`　值：你的 Gemini 金鑰
   - （可選）名稱：`MODEL`　值：`gemini-2.5-flash`（不填就用這個預設）
5. 右上 **部署 → 新增部署作業 → 類型選「網頁應用程式」**：
   - 執行身分：**我自己**
   - 誰可以存取：**任何人**
6. 複製產生的網址（`https://script.google.com/macros/s/.../exec`）。

> 之後若改了 `Code.gs`，要 **部署 → 管理部署作業 → 編輯（鉛筆）→ 版本選「新版本」→ 部署**，網址不變。

## 二、接上前端

1. 手機/電腦瀏覽器打開 `n1-reading.html`（或上 GitHub Pages）。
2. 右上 **⚙️ 後端設定** → 貼上剛剛的網址 → **儲存** → **測試連線**，看到「連線成功 ✅」即可。

## 三、加到 iPhone 主畫面（PWA）

1. 用 **Safari** 打開上線網址（GitHub Pages 的 `n1-reading.html`，要 https，不能是本機檔案）。
2. 底部 **分享鈕（□↑）→「加入主畫面」** → 命名「N1 読解」→ 加入。
3. 桌面會出現紫色 **N1 読解** 圖示（`n1-icon-180.png`），點開就是全螢幕、無網址列，像原生 App。

> ⚠️ **重要雷點**：iPhone 上「加到主畫面的 App」跟 Safari 的資料是**分開**的。
> 所以請**在桌面 App 裡面**（不是在 Safari）去 ⚙️ 貼後端網址、按儲存——這樣設定和生字本才會存在 App 自己的空間、下次開還在。
> 清除 Safari 資料不會動到桌面 App 的資料，反之亦然。

## 四、用法

- **📝 出文章**：選テーマ + **問題タイプ（本試験の大問）**→「文章を作る」。走 JLPT N1 読解風格、Gemini 生成。六大問全支援：
  - 問題8 内容理解・短文（~200字/1問）
  - 問題9 内容理解・中文（~500字/3問）
  - 問題10 内容理解・長文（~1000字/4問）
  - 問題11 統合理解（A・B 兩篇對照/3問）
  - 問題12 主張理解・長文（~1000字/4問）
  - 問題13 情報検索（案内・広告の掲示物＋条件照合/2問）
- **📖 読む**：
  - 語をタップ → 讀音 + 意味（繁中）。「分からない」→ 收進ノート。
  - **ふりがな** 開關預設關，右上一鍵開/關（純 CSS，瞬間切換）。
  - 每段可「この段落を訳す」→ 繁中翻譯。
  - 下方有重要語彙、N1 文法ポイント（可收進ノート）、読解問題（題目日文、解析繁中）。
- **📓 ノート**：単語＋文法一起管理，可標「習得／要復習」，用「要復習」的項目出復習テスト。
  每列有 **✏️** 可**追加/修正唸法（読み）**（例如查字時沒帶到讀音，事後自己補上）。

> 出文章時 Gemini 除了斷詞 token 還會回一份純文字全文；前端會**比對 token 接起來是否還原本文**，
> 對不上就自動退回純文字顯示（不漏字，但那篇暫時不能逐字點、無ふりがな），避免斷詞出包害整篇壞掉。
- **📅 チェックイン**：每日打卡週曆（單人）。答完読解問題會自動打卡。

## 檔案

| 檔案 | 用途 |
|---|---|
| `n1-reading.html` | 前端（單一 HTML，純 JS，PWA） |
| `n1-reading.webmanifest` | PWA manifest |
| `n1-apps-script/Code.gs` | 後端（Gemini 代打 + Sheets 儲存） |

## 核心設計：Gemini 出文章時一起斷詞

日文沒有空白斷詞。`generateArticle` 讓 Gemini 回傳**形態素 token 陣列**
（表層形 `s` + 讀音 `r` + 詞性 `p` + 意味 `zh` + 重點旗標 `k`，段落用 `{"br":1}`），
前端不需要任何斷詞器，直接逐 token 渲染成可點的字，`r` 存在就組 `<ruby>` 標音。
