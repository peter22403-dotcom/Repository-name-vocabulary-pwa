# 📖 會考閱讀助手 — 設定教學

給爸爸看的一次性設定步驟。做完之後，小孩打開網頁就能用，你不用再碰後端。

整個流程分三段：
1. 拿 Gemini 金鑰
2. 建 Google Sheet + 貼 Apps Script（金鑰藏這裡）
3. 把後端網址填進網頁

預計 15 分鐘。

---

## 第一段：拿 Gemini API 金鑰

1. 打開 <https://aistudio.google.com/apikey>（用你的 Google 帳號登入）
2. 按 **Create API key**（建立 API 金鑰）
3. 複製那串金鑰（長得像 `AIzaSy....`），先貼到記事本備用

> 費用：`gemini-2.5-flash` 很便宜，一家小孩天天用大概一個月幾十塊台幣以內，甚至可能落在免費額度內。

---

## 第二段：建 Google Sheet + Apps Script

### 2-1 建一份新的 Google Sheet
1. 打開 <https://sheets.new>（會直接開一份新試算表）
2. 幫它取個名字，例如「小孩閱讀資料」

### 2-2 開 Apps Script
1. 在這份 Sheet 上方選單：**擴充功能 → Apps Script**
2. 會開一個新分頁，裡面有個 `Code.gs` 檔，內容預設是空的 `function myFunction(){}`
3. 把裡面的東西全部刪掉
4. 打開本專案的 `apps-script/Code.gs`，**整份複製貼上**到剛剛清空的地方
5. 按上方的 💾 **儲存**（或 Ctrl/Cmd + S）

### 2-3 把 Gemini 金鑰藏進去
1. Apps Script 左邊齒輪 ⚙️ **專案設定**
2. 往下找到 **指令碼屬性 (Script properties)** → **新增指令碼屬性**
3. 填：
   - 屬性：`GEMINI_API_KEY`
   - 值：貼上你第一段複製的金鑰
4. （可選）再加一個：屬性 `MODEL`，值 `gemini-2.5-flash`（不加就用預設，一般不用加）
5. 按 **儲存指令碼屬性**

### 2-4 部署成網頁應用程式
1. 右上角 **部署 → 新增部署作業**
2. 齒輪選 **網頁應用程式 (Web app)**
3. 設定：
   - 說明：隨便打，例如「閱讀助手 v1」
   - 執行身分：**我 (你自己的帳號)**
   - 誰可以存取：**任何人 (Anyone)**  ← 一定要選這個，網頁才連得到
4. 按 **部署**
5. 第一次會要你 **授權 (Authorize)** → 選你的帳號 → 若跳「Google 尚未驗證這個應用程式」，點「進階 → 前往（不安全）」（這是你自己寫的程式，安全）→ 允許
6. 完成後會給你一個 **網頁應用程式網址**，長得像：
   ```
   https://script.google.com/macros/s/AKfyc..../exec
   ```
   **複製這個網址**

> 💡 之後如果改了 `Code.gs`，要重新「部署 → 管理部署作業 → 編輯(鉛筆) → 版本選新版本 → 部署」，網址不會變。

---

## 第三段：把網址填進網頁

1. 打開閱讀助手網頁（GitHub Pages 上線後網址是）：
   ```
   https://peter22403-dotcom.github.io/Repository-name-vocabulary-pwa/reading.html
   ```
2. 按右上角 **⚙️ 後端設定**
3. 把第二段複製的網址貼進去 → 按 **儲存** → 按 **測試連線**
4. 看到「連線成功 ✅」就完成了！

---

## 開始用

1. **📝 出文章**：選主題、長度 → 按「✨ 出一篇文章」（等 10-20 秒）
2. **📖 讀文章**：
   - 點任何一個英文字 → 看中文意思
   - 覺得不會的字 → 彈窗按「➕ 我不會，加入生字本」，字會變黃色
   - 下面有重點單字、文法、閱讀測驗（作答後按「對答案」看解析）
3. **📓 生字本**：看累積的生字，可按「🎯 開始生字小考」複習；背熟了按「標為已熟」

資料（生字本、測驗成績）都存在你的 Google Sheet，三個小孩分開記錄。

---

## 加到 iPad / 手機主畫面（像 App 一樣）
1. 用 **Safari** 開上面的 reading.html 網址
2. 按分享 → **加入主畫面**
3. 之後從主畫面圖示打開就好

---

## 常見問題

- **測試連線失敗？** 檢查「誰可以存取」有沒有選「任何人」；網址結尾要是 `/exec` 不是 `/dev`。
- **出文章失敗、說金鑰錯誤？** 回 Apps Script 專案設定，確認 `GEMINI_API_KEY` 有貼對、沒有多空白。
- **三個小孩資料混在一起？** 網頁上方「👦 目前小孩」要先選對人再操作。
- **改了 Code.gs 沒生效？** 要重新部署新版本（見第二段最後的提示）。
